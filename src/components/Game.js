import Board from './Board';

const Game = () => {
  const handleClick = () => {};

  const jumpTo = () => {};

  const renderMoves = () => {};

  return <Board handleClick={handleClick} />;
};

export default Game;
